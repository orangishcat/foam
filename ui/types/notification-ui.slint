export struct Notification { title: string, course: string, course_id: string }

export struct NotificationUi {
    progress: float,
    temp-notifs: [Notification],
    notifications: [Notification],
}
