export struct Assignment {
    id: string,
    title: string,
    course-id: string,
    course-name: string,
    done: bool,
    overdue: bool
}

export struct AssignmentCol {
    title: string,
    assignments: [Assignment],
}
