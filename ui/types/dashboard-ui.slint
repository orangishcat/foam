export struct Assignment { title: string, course_id: string, course_name: string, color: color }

export struct AssignmentCol {
    title: string,
    assignments: [Assignment],
}

export struct DashboardUi {
    assignment-view: [AssignmentCol],
}
