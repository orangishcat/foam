export struct Assignment { title: string, course-id: string, course-name: string, done: bool, overdue: bool }

export struct AssignmentCol {
    title: string,
    assignments: [Assignment],
}

export struct DashboardUi { assignment-view: [AssignmentCol], selected-assignment: Assignment }
